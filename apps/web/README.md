# PeopleSyncD Web Application

Planned structure: `src/`, `components/`, `pages/`, `services/`, `tests/`, and `accessibility/`.

The web client consumes versioned APIs and shared packages. It does not contain independent authorization logic that replaces server enforcement.
