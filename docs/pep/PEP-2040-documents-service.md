# PEP-2040: Documents Service

- Status: Accepted

The Documents service owns metadata, versions, classification, access rules, checksums, private storage references, signature status, retention, expiration, legal hold, and secure delivery. Signed versions are immutable. Bytes remain in protected object storage and are never public by default. Initial requirement namespace: `REQ-DOC-*`.
