# Database Specifications

Database contracts define schemas, migrations, constraints, row-level security, indexes, views, retention, compatibility, backup implications, and rollback or forward-fix guidance.

Service implementations conform to database contracts and do not silently redefine them.
