# Backup and Restore Operations

Backups cover PostgreSQL, private object metadata and bytes, configuration history, audit evidence, and required infrastructure state according to the deployment model.

Every backup has encryption, retention, integrity verification, ownership, and deletion rules. Restoration exercises occur in isolated environments and validate tenant counts, row-level policies, object integrity, audit continuity, configuration, and application health.
