# Domonique 2.0 Tool Registry

Every registered tool declares permanent ID, version, owner, purpose, service endpoint, input and output schemas, permission, tenant scope, risk class, approval requirement, idempotency, timeout, retry, rate limit, audit events, data classification, and rollback behavior.

Tools are disabled by default until approved. A model cannot invent, modify, or invoke an unregistered tool.
