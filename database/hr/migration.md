# HR Database Migration

Order: `schema.sql`, `constraints.sql`, `indexes.sql`, `views.sql`.

The SQL files are a foundation contract, not an authorized production migration. Production use requires a versioned migration tool, backup confirmation, compatibility testing, validation queries, rollback or forward-fix strategy, and release certification.
