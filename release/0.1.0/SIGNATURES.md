# Genesis 0.1.0 Signatures

No release signature or Windows code signature exists. Private signing material must never be committed. The signing workflow fails closed unless the protected owner-controlled signing key is configured.
