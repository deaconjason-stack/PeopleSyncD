apiVersion: v1
kind: Service
metadata:
  name: peoplesyncd-web
  namespace: peoplesyncd
spec:
  selector:
    app.kubernetes.io/name: peoplesyncd-web
  ports:
    - name: http
      port: 80
      targetPort: http
