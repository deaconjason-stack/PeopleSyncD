apiVersion: v1
kind: Service
metadata:
  name: peoplesyncd-api
  namespace: peoplesyncd
spec:
  selector:
    app.kubernetes.io/name: peoplesyncd-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
