# Person Permissions

`person.read.summary`, `person.read.sensitive`, `person.create`, `person.update`, `person.merge.review`, and `person.history.read`. Default is deny; sensitive access requires separate permission and audit.
