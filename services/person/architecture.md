# Person Architecture

Authenticated requests pass through organization-context validation, authorization, schema validation, domain rules, persistence, audit emission, and response filtering. Deduplication produces review candidates rather than autonomous merges.
