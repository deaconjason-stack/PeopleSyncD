# Person Database Contract

Owns `persons`, `person_names`, `person_contacts`, `person_change_history`, and `person_merge_reviews`. Every row carries organization scope where applicable. Highly sensitive values use additional encryption and are excluded from ordinary views.
