# Identity Telemetry

Metrics: authentication attempts, failure rate, MFA challenges, lockouts, recovery attempts, session issue and revocation, federation failures, latency, and dependency health. Logs and traces use correlation IDs and exclude credentials and tokens.
