# Worker Audit Events

Audit actions include `worker.create`, `worker.read`, `worker.update`, `worker.end`, `worker.archive`, `worker.authorization_denied`, and `worker.transition_failed`. Evidence records actor, organization, action, target, result, reason, correlation ID, AI involvement, and timestamp. Sensitive before/after values are minimized or encrypted according to classification.
