# Documents Database Contract

Owned objects: documents, versions, folders, tags, access rules, signature requests, retention policies, legal holds, scan results, delivery authorizations, and outbox records. Bytes reside in private object storage; the database stores protected references and checksums.
