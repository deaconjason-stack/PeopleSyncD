# Documents Architecture

Components: metadata API, upload authorizer, private object-storage adapter, malware scanner, checksum verifier, version manager, access evaluator, signature adapter, retention engine, search publisher, event outbox, and audit adapter.

Document bytes are never public by default and are not stored in application logs or event payloads.
