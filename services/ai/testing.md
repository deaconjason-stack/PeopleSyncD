# Domonique 2.0 AI Testing

Required suites cover tenant isolation, authorization, assistant-mode narrowing, grounding, citation accuracy, uncertainty, refusal, direct and indirect prompt injection, malicious documents, tool allowlisting, schema validation, approval, stale state, idempotency, memory controls, data deletion, legal hold, voice privacy, model outage, kill switch, bias, accessibility, latency, and cost budgets.

Every test receives a permanent `PSD-TEST-AI-*` identifier before release certification.
