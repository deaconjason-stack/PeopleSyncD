# HR Database Contract

The HR service owns tenant-scoped workforce relationship data. Canonical Worker SQL currently resides under `database/worker/` and will migrate into versioned service migrations after ADR approval.
