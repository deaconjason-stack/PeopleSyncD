# Permissions Domain

Entities: Role, Permission, Policy, Scope, Grant, Deny Rule, Obligation, Decision, Policy Version.

Grant states: `Proposed → Active → Expired or Revoked`.

Decision result: allow or deny, reason code, scope, obligations, policy version, and correlation identifier.
