# Permissions Requirements

- `PSD-REQ-PERM-000001` Default-deny authorization.
- All policy decisions are tenant-scoped, versioned, explainable, and auditable.
- Temporary grants expire automatically.
- Client-side visibility rules never replace service enforcement.
