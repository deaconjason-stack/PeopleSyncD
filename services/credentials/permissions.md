# Credentials and Training Permissions

`credential.read.own`, `credential.read.manage`, `credential.create`, `credential.verify`, `credential.renew`, `training.assign`, `training.complete`, and `training.verify`.
