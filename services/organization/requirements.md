# Organization Requirements

- `PSD-REQ-ORG-000001` Explicit organization context.
- `PSD-REQ-ORG-000002` Governed hierarchy and membership.

Every hierarchy rejects cycles, every membership is effective-dated, and parent-child relationships do not automatically grant data access.
