# Organization Permissions

Permissions include organization read, hierarchy manage, membership invite, membership end, settings read, settings manage, and context activate. Context activation requires an active membership and cannot be delegated by client input alone.
