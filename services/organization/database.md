# Organization Database Contract

Owned objects: organizations, hierarchy nodes, memberships, settings, locations, business units, cost centers, and outbox records. Hierarchy constraints prevent cycles. Tenant identifiers are stable and never reused.
