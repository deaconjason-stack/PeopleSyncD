# Workflow Database Contract

Owned objects: definitions, definition versions, instances, states, transition history, tasks, approvals, deadlines, reminders, exceptions, compensation actions, execution locks, and outbox records. Historical instances retain their definition version.
