# Workflow Telemetry

Metrics: active instances, transition latency, rejection rate, task age, overdue tasks, approval age, retry and compensation count, scheduler delay, event backlog, and workflow failure rate. Correlation identifiers link workflow actions across services.
